using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeightMouvementTab : MonoBehaviour
{
    #region Editable fields

    [SerializeField] string emotion = "neutral";
    [SerializeField] int head_weight = 1;
    [SerializeField] int uppertorse_weight=1;
    [SerializeField] int lowertorse_weight=1;
    [SerializeField] int shoulders_weight = 1;


    #endregion
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
